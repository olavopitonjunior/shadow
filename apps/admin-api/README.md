# Shadow Admin API (Local)

## Requirements
- Python 3.11+
- Variables: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`

## Run

```bash
pip install -r requirements.txt
uvicorn main:app --host 127.0.0.1 --port 8099
```
