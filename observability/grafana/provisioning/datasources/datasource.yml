# Grafana Datasource Provisioning
# Configura automaticamente o Prometheus como fonte de dados

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
